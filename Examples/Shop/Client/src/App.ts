// Entry point for application.
// Redirect to login page, preserving query string (e.g. test mode).

window.location.replace( "Login/Login.html" + window.location.search );
// window.location.replace( "Product/Product.html" + window.location.search );
// window.location.replace( "Order/Order.html" + window.location.search );
